# Expected Behavior

- A grid background with a transformed rectangle is visible in the left viewport.
- Dragging gizmo handles updates the rectangle and status text in real time.
- PropertyGrid edits update the same transform without desync.
- Snap controls constrain translate/rotate/scale increments when set above zero.
