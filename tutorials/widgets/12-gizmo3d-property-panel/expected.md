# Expected Behavior

- A perspective viewport shows a ground grid and a wireframe cube.
- `Gizmo3D` overlays the cube and supports translate/rotate/scale interactions.
- PropertyGrid values stay synchronized with gizmo interaction.
- Camera controls (yaw/pitch/distance) update the viewport projection immediately.
