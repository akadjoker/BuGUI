# 06 - Menus & Dialogs

Goal: validate menu flows, popup ownership, and dialog actions.

Covered widgets:
- `MenuBar`
- `ContextMenu`
- `AlertDialog`
- `ConfirmDialog`
- `InputBox`

Run:

```bash
./bin/tutorials/widgets/bugui_tutorial_widgets_06_menus_dialogs
```

Manual checks:
1. Use `File` and `View` menu actions and verify status updates.
2. Right-click panel and test context actions.
3. Open each dialog button and verify callbacks fire.
